export const config = {
    portas: {
        agendamento: number,
        sessao: number,
        usuarios: number
    },
    database: {
        host: "string",
        user: "string",
        password: "string",
        database: "string"
    }
};